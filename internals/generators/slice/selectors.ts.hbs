import { RootState } from '@src/types/RootState'
import { initialState } from './reducer'

const select{{ camelCase sliceName }} = (state: RootState) => state.{{ camelCase sliceName }} || initialState
